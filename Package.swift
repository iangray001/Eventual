// swift-tools-version:4.2

import PackageDescription

let package = Package(
    name: "Eventual",
    dependencies: [
    ],
    targets: [
        .target(
            name: "Eventual",
            dependencies: [ ])
    ]
)
